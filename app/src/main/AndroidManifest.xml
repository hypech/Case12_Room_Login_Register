<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hypech.case9login_room">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Case7Core_SQLite">
        <activity android:name="com.hypech.case9login_room.BusinessLogic"></activity>
        <activity android:name="com.hypech.case9login_room.Register" />
        <activity android:name="com.hypech.case9login_room.Login" />
        <activity android:name="com.hypech.case9login_room.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>